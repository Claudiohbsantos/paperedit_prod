import*as utils from"../modules/utils.js";import player from"../modules/player.js";import{userSettings as settings}from"../modules/userSettings.js";import{Component}from"./component.js";class GoToTime extends Component{constructor(){super(document.getElementById("goto-prompt")),this.attachListeners()}attachListeners(){document.getElementById("tinput-gototime").addEventListener("keydown",t=>this.submit(t)),document.getElementById("tinput-gototime").addEventListener("change",()=>this.hide())}submit(t){if("Enter"===t.key){let e=document.getElementById("tinput-gototime").value.trim(),o=utils.parseTimeInput(e,settings.framerate);void 0!==o&&player.seekToSec(o),this.hide(),t.target.value=""}}}let singleton=new GoToTime;export default singleton;